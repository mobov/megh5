{
  "name": "megh5",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "creator:serve": "lerna run serve --scope @megh5/creator --stream",
    "creator:package": "lerna run build --scope @megh5/creator --stream",
    "creator:deploy": "lerna run deploy --scope @megh5/creator --stream",
    "ui:serve": "lerna run serve --scope @megh5/ui --stream",
    "ui:build": "lerna run build --scope @megh5/ui --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@mobov/eslint-config": "^1.0.1",
    "lerna": "^3.13.1",
    "typescript": "^3.4.3"
  }
}
